<?php
return [
    /*
     * The name of the cookie which is stored after consent is given
     */
    'cookie_name' => 'cookie_consent',

    /*
     * The name of the cookie which is stored after consent is given
     */
    'cookie_value' => 'yes',

    /*
     * The name of the cookie which is stored after consent is given
     */
    'cookie_lifetime' => 60*24*365,

    /*
     * Include cookie policy link and modal
     */
    'show_cookie_policy' => false,

    /*
     * Include privacy policy link and modal
     */
    'show_privacy_policy' => false,
];
